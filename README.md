## main-server-node
Special created server that uses MQTT for managing clients and rendering images and grab data about other nodes

### Set up environment

```
python3 -m venv env
source env/bin/activate 
pip3 install -r requirements.txt
```

### Running
```
python3 main.py
```
